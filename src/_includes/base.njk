<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ title }}</title>
	{% include '_google-fonts.njk' %}
	<link rel="stylesheet" href="{{ '/assets/styles/style.css' | url }}" />
	<script src="{{ '/assets/scripts/main.js' | url }}" defer></script>
</head>
<body>
    {% include '_navbar.njk' %}

	{{ content | safe }}
</body>
</html>
